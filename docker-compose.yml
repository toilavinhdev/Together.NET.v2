version: "3.9"

services:
  together-nginx:
    container_name: together-nginx
    build:
      context: Together.Proxy
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped
  
  together-postgres:
    container_name: together-postgres
    image: postgres
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES-DB=Together.NET.v2
    ports:
      - "5432:5432"
    restart: unless-stopped
    volumes:
      - postgres-data:/var/lib/postgresql/data/
    
  together-redis:
    container_name: together-redis
    image: redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - redis-data:/data
  
  together-api:
    container_name: together-api
    build:
      context: Together.Server
      dockerfile: Together.API/Dockerfile
    ports:
      - "8005:5005"
    restart: unless-stopped
    depends_on:
      - together-postgres
      - together-redis
    
volumes:
  postgres-data:
  redis-data:
